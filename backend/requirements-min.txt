# Default (SQLite) runtime dependencies.
#
# This file is the only requirements file installed by the default image.
# Optional features live in their own files and are opt-in:
#
#   -r requirements-postgres.txt   PostgreSQL (Psycopg 3 only)
#   -r requirements-redis.txt      Redis (multi-instance deployments)
#   -r requirements-azure.txt      Azure / Microsoft Entra ID auth
#   -r requirements-ldap.txt       LDAP authentication
#   -r requirements-optional.txt   all of the above
#
# Keep this list lean: everything here is paid for by every deployment.
#
# starlette-compress hard-depends on zstandard; the Dockerfile removes that
# package and main.py disables zstd compression (brotli + gzip remain), which
# saves ~23 MB of image size with no client-visible change.
#
# starlette-compress hard-depends on zstandard; the Dockerfile removes that
# package and main.py disables zstd compression (brotli + gzip remain), which
# saves ~23 MB of image size with no client-visible change.

fastapi==0.136.3
uvicorn==0.51.0
uvloop==0.22.1
httptools==0.8.0
websockets==17.1
pydantic==2.13.4
python-multipart==0.0.32
itsdangerous==2.2.0

python-socketio==5.16.2
orjson==3.11.9
cryptography==48.0.0
bcrypt==5.0.0
argon2-cffi==25.1.0
PyJWT[crypto]==2.13.0
authlib==1.7.2
joserfc==1.7.4

# requests is imported by python-engineio's client module, which
# python-socketio imports eagerly.
requests==2.34.2
# httpx is not imported by open_webui itself, but authlib's starlette_client
# integration imports it; OAuth breaks without it. Kept at its default extras
# (no http2/socks/zstd) to stay small.
httpx==0.28.1
aiohttp==3.13.5
aiocache==0.12.3
aiofiles==25.1.0
starlette-compress==1.7.1
Brotli==1.2.0
starsessions==2.2.1

sqlalchemy[asyncio]==2.0.50
aiosqlite==0.22.1
alembic==1.18.4

loguru==0.7.3

# Extras: see requirements-pdf.txt / requirements-code-format.txt /
# requirements-pillow.txt and the ENABLE_* build args in the Dockerfile.
